import { NgModule } from '@angular/core';
import { IonicPageModule} from 'ionic-angular';
import { CompruebaPage } from './comprueba';

@NgModule({
  declarations: [
    CompruebaPage,
  ],
  imports: [
    IonicPageModule.forChild(CompruebaPage),
  ],
})
export class CompruebaPageModule {}
